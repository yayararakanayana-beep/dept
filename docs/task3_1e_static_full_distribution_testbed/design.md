# Task 3.1e Design

Builds a static full-distribution PseudoReality v3.3 testbed from the real `DistributionTerrainV322World` path with `n_bins=5`. The design keeps G_t candidates as opaque 3125-cell static probability snapshots, separates discovery metadata from audit metadata, and does not select semantic axes or implement dynamic G_t timing.
