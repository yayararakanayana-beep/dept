# Task 3.1e Dataset Contract

Artifacts are generated under `artifacts/pseudoreality_v3_3_task3_1e_static_full_distribution/`.

The generator writes raw vectors, adaptive pool, selected additions, snapshots, exact base pairs, coordinates, mass, terrain, and generation metadata. The independent auditor writes measured before/after adaptive Jensen–Shannon statistics. The validator writes evidence-rich checks, results, and a type-aware SHA-256 manifest.

`discovery_manifest.csv` has exactly `matrix_row_index`, `snapshot_id`, `dataset_split`, and `analysis_weight`.

Formal: 147 initial fit + 32 adaptive, 84 validation, 80 holdout, 178 candidates, 1582 snapshots. Smoke: 8 initial fit + 2 adaptive, 2 validation, 4 holdout, 8 candidates, 41 snapshots.
