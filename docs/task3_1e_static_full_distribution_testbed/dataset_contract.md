# Task 3.1e Dataset Contract

Artifacts are generated under `artifacts/pseudoreality_v3_3_task3_1e_static_full_distribution/`. The discovery manifest contains only `matrix_row_index`, `snapshot_id`, `dataset_split`, and `analysis_weight`. External-factor values are stored only in `external_vectors.csv`; source-step and seed details are stored only in audit metadata.
