# Task 3.1e Results

Formal numerical results are generated by GitHub Actions and are not committed. Both profiles execute raw generation, independent coverage audit, and independent strict validation. A formal run can be requested before merge with the `run-task3-1e-formal` label.
