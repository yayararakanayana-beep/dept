name: Task2-8j-4c Seed Rolling Stability

on:
  workflow_dispatch:

jobs:
  task2-8j-4c:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install pytest
      - name: Run tests
        env:
          PYTHONPATH: localprep1/dept:localprep1/dept/DEPT2_ActionModule_ActuationPrimitives_RC1
        run: pytest -q tests/test_action_module_upper_pressure_reception_split_task2_8j_4c.py
      - name: Export tables
        if: always()
        env:
          PYTHONPATH: localprep1/dept:localprep1/dept/DEPT2_ActionModule_ActuationPrimitives_RC1
        run: |
          python - <<'PY'
          from pathlib import Path
          import json
          from dept2_fullspec_runner_rc1.modules.action_policies.pressure_action_task2_8j_1_candidate_feature_log import CandidateFeatureLogConfig
          from dept2_fullspec_runner_rc1.modules.action_policies.pressure_action_task2_8j_4b_4c_4d_4e_prefreeze_validations import PreFreezeValidationConfig, build_and_validate_4c_seed_rolling_stability
          out = Path('results/task2_8j_4c_seed_rolling_stability_rc1')
          out.mkdir(parents=True, exist_ok=True)
          feature_cfg = CandidateFeatureLogConfig(steps=24, seeds=(501, 502, 503), window_sizes=(1, 6, 12))
          cfg = PreFreezeValidationConfig(component_counts=(3, 6, 7, 8), standard_component_count=6, shadow_component_count=7)
          table, final, errors, summary = build_and_validate_4c_seed_rolling_stability(feature_cfg, cfg)
          table.to_csv(out / 'seed_rolling_stability.csv', index=False)
          final.to_csv(out / 'final_summary.csv', index=False)
          (out / 'summary.json').write_text(json.dumps(summary, ensure_ascii=False, indent=2), encoding='utf-8')
          (out / 'TASK2_8J_4C_SEED_ROLLING_STABILITY_RC1.md').write_text('# Task 2-8j-4c\n\n' + json.dumps(summary, ensure_ascii=False, indent=2), encoding='utf-8')
          if errors:
              raise SystemExit('Task2-8j-4c validation errors')
          PY
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: task2-8j-4c-seed-rolling-stability-rc1
          path: results/task2_8j_4c_seed_rolling_stability_rc1/
          if-no-files-found: error
